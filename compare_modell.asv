%% This script is used for comparing the models to eachother:

original_path_prefix = "Images/Original/";
method = "Gaussian";
reprod_path_prefix = "Images/Reproduction/"+ method + "/";

Quality_index = [];
SSI_score= [];

for i=1:5
    original_path = 
    Quality_index = [Quality_index, image_score(, reprod_path_prefix + )]
    SSI_score = [SSI_score, ssim()]
end